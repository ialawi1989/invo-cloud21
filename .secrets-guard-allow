# Secrets guard allowlist — scripts/check-secrets.sh
#
# ONE purpose: pre-existing, already-ticketed violations that cannot be fixed
# yet because remediation is sequenced behind credential rotation. Everything
# listed here is STILL EXPOSED and is reported on every run, not silenced.
#
# This is not a place to put a new file so a commit will go through. If you
# are adding a line because the guard blocked you, stop — the guard is right.
#
# Format: one shell glob per line, matched against the repo-relative path.
# Every entry needs a reason and a ticket.

# ---------------------------------------------------------------------------
# website/.env — tracked since bb2204e, 2026-06-08.
#
# Contents are subDomain, domain and BASE_URL. No credentials, verified
# 2026-08-11. Listed as low severity so the guard does not block unrelated
# work on every commit.
#
# It is NOT harmless: untracking it is deliberately sequenced after the AWS
# and Sentry rotations (see the tickets in InvoCloudBack). Remove this line
# at that point — do not leave it here once the file is untracked.
#
#   docs/tickets/tracked-env-files-across-repos.md  §3
# ---------------------------------------------------------------------------
website/.env
