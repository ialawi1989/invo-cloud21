# Copy this to .env.local (gitignored) to override the shared .env for your
# own machine — e.g. a LAN IP instead of the shared dev backend. Re-run
# `npm start`/`npm run build` after changing it; see scripts/apply-env.mjs.

DEV_BACKEND_URL=http://10.2.2.89:3001/v1/app/
