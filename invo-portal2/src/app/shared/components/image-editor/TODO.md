# Pending improvements

## Media Picker - Multi-select mode
1. **Sidebar should show list of ALL selected items** (not just one) with thumbnails + info
2. **If no space**: show selected count + a scrollable list
3. **Pre-select existing images** when reopening picker (pass current selection via config)
4. **Edit button on each selected item** in sidebar (opens image editor)
5. **Reorder selected items** via drag & drop

## Image Editor
1. Crop drag handles need mouse event wiring (currently static)
2. Add eraser tool
3. Add undo limit (currently unbounded)

## Upload Toast
1. Show real upload progress (currently simulated 10% → 40%)
2. Cancel individual uploads mid-flight
